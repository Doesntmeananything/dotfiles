- defaults:
    link:
      create: true
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.doom.d:  

- shell:
  - [git submodule update --init --recursive, Installing submodules]
